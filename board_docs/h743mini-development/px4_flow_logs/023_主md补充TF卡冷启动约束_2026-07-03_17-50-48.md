# 023 主 md 补充 TF 卡冷启动约束

时间：2026-07-03 17:50:48

## 任务目标

把当前 H743mini 主文档 `px4_flow.md` 里关于 TF 卡的阶段性约束写到最前面，并调整主 md 结构，避免后续继续把 `reboot` / 板载复位键后的 TF 卡状态误当成当前阶段的正常判断依据。

## 当前 Git 状态

```text
## h743mini-v1.13.3...fork/h743mini-v1.13.3
 M .vscode/settings.json
 M boards/gjl/h743mini/init/rc.board_defaults
?? WARN
?? dmesg
```

说明：

- `.vscode/settings.json`、`boards/gjl/h743mini/init/rc.board_defaults`、`WARN`、`dmesg` 都是本轮文档整理前就已经存在的工作区状态。
- 本轮不修改 PX4 源码和构建配置，只更新 Obsidian 主 md 与进展记录。

## 最近一次提交

```text
c21d64251c (HEAD -> h743mini-v1.13.3, fork/h743mini-v1.13.3) refactor: 将原led引脚迁移至新gpio,分别为PI11(RED)、PI8(BULE)、PC13(GREEN)
```

## 本轮修改内容

本轮修改集中在：

```text
/home/gjl/Documents/Obsidian Vault/PX4二次开发/px4_flow.md
/home/gjl/Documents/Obsidian Vault/PX4二次开发/px4_flow_logs/023_主md补充TF卡冷启动约束_2026-07-03_17-50-48.md
```

主 md 结构调整点：

- 在文档最前面补充“当前阶段最重要的约束”，明确 TF 卡、`/fs/microsd`、`params`、`dataman`、`logger` 相关验证都应以完全断电冷启动结果为准。
- 调整“阅读顺序建议”，把“当前阶段启动约束”提到最前。
- 更新“当前状态”段落，写明 TF 卡冷启动可用、`reboot` / 板载复位键后不可靠。
- 更新“当前主要问题”和“下一阶段任务和目标”，让表述与当前真实进展一致。
- 在“关键结论”中加入 TF 卡冷启动约束。
- 在“进展索引”中追加本条子 md。

## 修改流程

1. 重新读取主 `px4_flow.md` 顶部结构，确认当前“阅读约定 / 当前状态 / 下一阶段任务和目标”的组织方式。
2. 重新对照 `020_TF卡读取问题调试复盘`，确认当前已验证结论是：
   - 完全断电后重新上电，TF 卡可挂载；
   - `reboot` / 板载复位键后，TF 卡状态不可靠。
3. 把这条阶段性约束提升到主 md 最前面，而不是只留在历史子 md 中。
4. 同步更新主 md 中已经过时的阶段状态表述，避免后续续接时继续误判。

## 关键输出

本轮没有重新编译或烧录，关键输出是文档层面的结构调整结果：

- 主 md 第一屏就能看到 TF 卡冷启动约束。
- 主 md 当前状态已明确：
  - 冷启动时 TF 卡、`params`、`dataman`、`logger` 可用；
  - `reboot` / 板载复位键后不可靠。
- 后续继续工作时，优先阅读“当前阶段启动约束”，减少对 TF 卡状态的误判。

## 当前结论

- 当前 H743mini 的 TF 卡链路不是“整体不可用”，而是“当前阶段只在完全断电冷启动场景下可靠”。
- 这条约束已经上升为主 md 的顶层上下文，不再只埋在 `020 TF 卡读取问题调试复盘` 子 md 中。
- 主 md 已调整为更适合续接当前阶段工作的结构：先看启动约束，再看当前状态，再看下一阶段任务。

## 下一阶段任务和目标

目标：在“TF 卡冷启动可用、`reboot` / 复位键后不可靠”这个已知约束下，继续清理 V6C 默认启动项，让 H743mini 最小系统日志更干净。

优先任务：

- 临时关闭当前未接入的 V6C 默认传感器启动项。
- 视需要处理 `No autostart ID found` 场景下的 `ekf2 start failed (-1)`。
- 继续保留 USB/QGC/NSH 调试链路和当前已验证的 TF 卡冷启动链路。

验收标准：

- 主 md 顶部结构能明确提醒后续先看 TF 卡冷启动约束。
- 后续继续调试时，不再把 `reboot` / 板载复位键后的 TF 卡状态误当作当前阶段的正常基线。
- 下一轮源码修改仍能围绕“清理 V6C 默认启动项”主线推进，而不是重新绕回 TF 卡软复位问题。
